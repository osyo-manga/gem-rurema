module REXML::EntityConst
  # --- AMP -> REXML::Entity
  # 「&amp;」「&」というエンティティを表わすオブジェクト。
  def AMP; end

  # --- APOS
  # 「&apos;」「'」というエンティティを表わすオブジェクト。
  def APOS; end

  # --- GT -> REXML::Entity
  # 「&gt;」「>」というエンティティを表わすオブジェクト。
  def GT; end

  # --- LT -> REXML::Entity
  # 「&lt;」「<」というエンティティを表わすオブジェクト。
  def LT; end

  # --- QUOT -> REXML::Entity
  # 「&quot;」「"」というエンティティを表わすオブジェクト。
  def QUOT; end

end
